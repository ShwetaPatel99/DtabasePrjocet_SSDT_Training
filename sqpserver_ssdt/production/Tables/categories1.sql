CREATE TABLE [production].[categories1] (
    [category_id]   INT           NOT NULL,
    [category_name] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_CategoryId_Categories1] PRIMARY KEY CLUSTERED ([category_id] ASC)
);

